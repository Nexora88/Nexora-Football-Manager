(()=>{const K='nexoraCareer',R=()=>JSON.parse(localStorage.getItem(K)||'null'),W=s=>localStorage.setItem(K,JSON.stringify(s));function draw(){const s=R(),c=s?.gameState?.domesticCup;if(!s||!c)return;const ties=c.rounds?.[c.round]||[];s.gameState.media??={news:[]};s.gameState.media.news.unshift({date:s.gameState.date,tag:'CUP DRAW',market:'LOCAL',title:`${c.name} KURASI CANLI`,detail:`${c.round}: ${ties.slice(0,6).map(x=>x.home+' — '+x.away).join(' · ')}`});s.gameState.media.news=s.gameState.media.news.slice(0,20);W(s);document.getElementById('cupDrawLive')?.remove();const e=document.createElement('div');e.id='cupDrawLive';e.className='draw-night';e.innerHTML=`<div class="draw-card"><span class="eyebrow">NEXORA SPORTS NETWORK · LIVE TV</span><h1>${c.name}</h1><div class="draw-ball">●</div><div class="draw-caption">${c.round} KURASI</div><div class="draw-pair">${ties.map(x=>`<span>${x.home} <b>VS</b> ${x.away}</span>`).join('')}</div><button class="secondary" id="closeCupDraw">KAPAT</button></div>`;document.body.appendChild(e);e.querySelector('#closeCupDraw').onclick=()=>e.remove()}window.NEXORA_CUP_DRAW={draw};window.addEventListener('nexora:cup-updated',()=>setTimeout(draw,80));})();
