(()=>{let layer=null,canvas=null,ctx=null,raf=0,closeTimer=0,particles=[];function open(reason='career'){try{if(window.NEXORA_SKIP_INTRO)return;if(layer)return;layer=document.createElement('div');layer.className='airport-vip';layer.innerHTML='<div class="airport-bg"></div><div class="airport-card"><button class="airport-close" aria-label="Close">×</button><div class="airport-head"><span>NEXORA / VIP TERMINAL</span><b>TRANSFER GATE 01</b></div><div class="ataturk-frame"><div class="ataturk-portrait">MKA</div><div class="ataturk-quote">Muhtaç olduğun kudret,<br>damarlarındaki asil kanda mevcuttur!</div></div><div class="airport-status"><span>● VIP TRANSFER</span><b id="airportReason"></b></div><canvas id="airportFx"></canvas></div>';document.body.appendChild(layer);layer.querySelector('.airport-close').addEventListener('click',close,{once:true});document.getElementById('airportReason').textContent=reason;canvas=layer.querySelector('canvas');resize();particles=[];for(let i=0;i<24;i++)particles.push({x:Math.random()*canvas.clientWidth,y:canvas.clientHeight*.82,vx:(Math.random()-.5)*.7,vy:-.5-Math.random(),a:1,c:i%2?'#63ff76':'#e51d2a'});raf=requestAnimationFrame(draw);clearTimeout(closeTimer);closeTimer=setTimeout(close,2250)}catch(error){console.error('[NEXORA] airport open failed',error)}}function close(){try{clearTimeout(closeTimer);cancelAnimationFrame(raf);layer?.remove();layer=null;canvas=null;ctx=null;particles=[];const d=document.getElementById('dashboard'),h=document.querySelector('.hero');if(d)d.hidden=false;if(h)h.hidden=true;window.NEXORA_I18N?.refresh?.();window.NEXORA_HOME_I18N?.refresh?.()}catch(error){console.error('[NEXORA] airport close failed',error)}}function resize(){if(!canvas)return;const ratio=window.devicePixelRatio||1;canvas.width=canvas.clientWidth*ratio;canvas.height=canvas.clientHeight*ratio;ctx=canvas.getContext('2d');ctx.setTransform(ratio,0,0,ratio,0,0)}function draw(){if(!ctx||!layer)return;ctx.clearRect(0,0,canvas.clientWidth,canvas.clientHeight);for(const p of particles){p.x+=p.vx;p.y+=p.vy;p.a-=.006;if(p.a<=0){p.y=canvas.clientHeight*.82;p.a=1}ctx.globalAlpha=p.a*.65;ctx.fillStyle=p.c;ctx.beginPath();ctx.arc(p.x,p.y,3+Math.random()*5,0,Math.PI*2);ctx.fill()}ctx.globalAlpha=1;raf=requestAnimationFrame(draw)}window.NEXORA_AIRPORT={open,close};window.addEventListener('nexora:major-transfer',e=>open(e.detail?.reason||'MAJOR TRANSFER'));window.addEventListener('resize',resize)})();