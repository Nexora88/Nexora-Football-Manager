(()=>{
  let audio=null,master=null;
  function audioReady(){try{if(audio)return true;audio=new (window.AudioContext||window.webkitAudioContext)();master=audio.createGain();master.gain.value=.045;master.connect(audio.destination);return true}catch{return false}}
  function tone(freq,duration=.08,type='sine',volume=.25){if(!audioReady())return;const o=audio.createOscillator(),g=audio.createGain();o.type=type;o.frequency.value=freq;g.gain.setValueAtTime(0,audio.currentTime);g.gain.linearRampToValueAtTime(volume,audio.currentTime+.01);g.gain.exponentialRampToValueAtTime(.001,audio.currentTime+duration);o.connect(g);g.connect(master);o.start();o.stop(audio.currentTime+duration+.02)}
  function crowd(level=1){const stage=document.getElementById('matchStage');if(!stage)return;stage.classList.remove('crowd-surge');void stage.offsetWidth;stage.style.setProperty('--crowd-level',level);stage.classList.add('crowd-surge');clearTimeout(stage.__crowdTimer);stage.__crowdTimer=setTimeout(()=>stage.classList.remove('crowd-surge'),900)}
  function eventFromLog(node){if(!node||node.dataset.fxBound)return;node.dataset.fxBound='1';const text=(node.textContent||'').toLowerCase();if(node.classList.contains('pass')){window.dispatchEvent(new CustomEvent('nexora:match-event',{detail:{type:'pass'}}));return}if(node.classList.contains('duel')){window.dispatchEvent(new CustomEvent('nexora:match-event',{detail:{type:'duel'}}));return}if(text.includes('şut')||text.includes('shot')){window.dispatchEvent(new CustomEvent('nexora:match-event',{detail:{type:'shot'}}))}}
  const observer=new MutationObserver(ms=>ms.forEach(m=>m.addedNodes.forEach(n=>{if(n.nodeType===1)eventFromLog(n)})));
  window.addEventListener('pointerdown',audioReady,{once:true});
  window.addEventListener('nexora:match-event',e=>{const x=e.detail||{};if(x.type==='goal'){crowd(1.5);tone(180,.18,'sawtooth',.5);setTimeout(()=>tone(320,.28,'square',.38),90)}else if(x.type==='pass'){tone(520,.025,'sine',.07)}else if(x.type==='duel'){crowd(.8);tone(120,.05,'triangle',.12)}else if(x.type==='shot'){tone(690,.06,'triangle',.12)}else if(x.type==='kickoff'){crowd(.75);tone(260,.12,'sine',.18)}else if(x.type==='coach-command'){tone(410,.04,'square',.1)}else if(x.type==='fulltime'){crowd(.9);tone(220,.16,'sine',.2);setTimeout(()=>tone(330,.2,'sine',.16),120)}});
  window.addEventListener('nexora:career-ready',()=>{const log=document.getElementById('matchLog');if(log)observer.observe(log,{childList:true})});
})();